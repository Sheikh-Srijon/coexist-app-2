export const metadata = {
  title: "Coexist - Login",
  description: "Coexist login page",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
